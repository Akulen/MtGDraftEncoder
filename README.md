## How to add a new set?

Edit `data/set_config.json` to add the new set following the other existing
extensions.
It might be necessary to delete
`data/{cards_mtga.csv,cards_scryfall.json,cards_scryfall.parquet}` if the new
extension is newer than those files.
